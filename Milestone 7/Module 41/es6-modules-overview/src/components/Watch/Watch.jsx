import * as Calculate from '../../utils/calculate';

function Watch() {

    const first = 55;
    const second = 199;
    const Sum = Calculate.Add(first, second);
    const Multi = Calculate.Multiply(first, second);
    const Div = Calculate.Divide(first, second);

    return (
        <div>
        
        </div>
    )
}

export default Watch
